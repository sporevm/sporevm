{
  "description": "malformed exec-form RUN fails during full-file parsing with an exact diagnostic",
  "expect": {
    "docker": "failure",
    "spore": "failure",
    "spore_diagnostic": "spore build: Dockerfile line 3: RUN exec form must be a non-empty JSON string array"
  },
  "scan_prefixes": []
}
